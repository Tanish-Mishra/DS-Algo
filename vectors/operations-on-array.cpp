#include <iostream>
#include <vector>

using namespace std;

int main() {

 // vector<int>arr = {1,3,4,2};

	// fill constructor 

   //	vector<int>arr(50,3); it initialised the vector arry with value 3 at the 50 indexes 

	// pop_back

	// arr.push_back(3);  // inserts the value 3 at the end of the vector 

	// arr.pop_back(); // removes the value at the end of the vector 

	// To get the size of the array how many elements are there 

	// int size = arr.size();

	// To get the capacity of the array not how many elements are there the capacity of the vector array 

	// int capacity = arr.capacity(); 

	// to print the vector

	// for(int i = 0;i<arr.size();i++){
	// 	cout << arr[i] << " ";
	// }  // or we can use the for each loop as well

	// for (int x : arr){
	// 	cout << x << " ";
	// }


	return 0;
}